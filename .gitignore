# Index / data — never commit a user's mail
*.sqlite
*.sqlite-*
*.db
data/
.local/

# Credentials / secrets / local config
*.env
.env*
*client_secret*.json
*credentials*
*token*.json
.encryption_key
config.local.*

# Operator-instance docs (private; never the generic tool)
*.local.md
INSTANCE.local.md

# Node
node_modules/
dist/
dist-worker/
dist-worker-bundle/
build/
*.tsbuildinfo
.npm/

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Workflow orchestration scaffolding (internal build artifacts)
.workflows/

# Internal milestone/PM planning (not public)
docs/SCOPE.md

# MCP bundle artifact (pack with `pnpm bundle`)
*.mcpb

# Per-worktree dev index (DB isolation, db.ts)
.mail-index-dev.sqlite*
